<div class="col-md-8">
	<% if @product.reviews.any? %>
	<h3>Reviews (<%= @product.reviews.count %>)</h3>
	<ol class="microposts">
	  <%= render @reviews %>
	</ol>
	<%= will_paginate @reviews %>
	<% end %>
</div>